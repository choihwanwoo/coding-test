# 코딩테스트 연습 - [PCCE 기출문제] 3번. 수 나누기 (LV.0)

## 풀이

**1. `number % 100` → 뒤에서 두 자리(나머지)를 구한다.**

`answer += number % 100` → 그 값을 answer에 더한다.

`number /= 100` → number를 100으로 나눈 몫으로 갱신해서 뒤 두 자리를 버린다.

이걸 for문으로 반복하면 두 자리씩 잘라 더하는 코드라고 생각하였다.

<span style="color:red">ex)</span> `number = 4859`

- 1번째: `59`를 더하고 number는 `48`
- 2번째: `48`를 더하고 number는 `0`
- 결과: `107`

<br>

**2. 반복 횟수는 자릿수에 맞춰야 한다.**

제한사항에서 `number`의 최댓값은 `2,000,000,000`(10자리)이고, 자릿수는 항상 2의 배수이다.

두 자리씩 자르면 최대 5번이면 충분하다.

그러므로 `i < 5`로 수정하면 된다고 생각하였다.

---

## 오답노트

**1. `i < 2`**

예시는 4자리라서 통과했지만, 6자리 이상이면 앞자리가 남아서 틀렸다.

테스트케이스 20개 중 일부만 맞았다.

<br>

**2. `i < 1000000000`**

통과는 했다. number가 0이 된 뒤에는 0만 더해져서 결과는 맞기 때문이다.

하지만 20억 번이나 도는 건 불필요하다.

`number` 제한이 20억인 것과 반복 횟수는 다르다. 필요한 반복은 최대 5번이다.

---

## 정답

```java
import java.util.Scanner;

public class Solution {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int number = sc.nextInt();
        int answer = 0;
        
        for(int i=0; i<5; i++){
            answer += number % 100;
            number /= 100;
        }

        System.out.println(answer);
    }
}
```
