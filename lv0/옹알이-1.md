# 코딩테스트 연습 - 옹알이 (1) (LV.0)

## 풀이

**1. 발음 가능한 단어는 `aya`, `ye`, `woo`, `ma`를 각각 최대 1번씩 이어 붙인 것이다**

`babbling` 배열에서, 이 조건을 만족하는 단어 개수를 세면 된다

<span style="color:red">ex)</span>

- `["aya", "yee", "u", "maa", "wyeoo"]` → `1` (`aya`만 가능)
- `["ayaye", "uuuma", "ye", "yemawoo", "ayaa"]` → `3`

<br>

**2. 단어에서 발음 4개를 지우고, 남는 글자가 없으면 가능하다**

```java
String[] words = {"aya", "ye", "woo", "ma"};
String temp = word;

for (String w : words) {
    temp = temp.replace(w, " ");
}

temp = temp.replace(" ", "");

if (temp.length() == 0) {
    answer++;
}
```

- `replace(w, " ")` → 해당 발음을 공백으로 바꿈
- 공백을 다 지우고 길이가 0이면 → 발음만으로 이루어진 단어

<span style="color:red">ex)</span> `"ayaye"`

- `aya` 지움 → `" ye"`
- `ye` 지움 → `"  "`
- 공백 제거 → `""` → 가능

<span style="color:red">ex)</span> `"yee"`

- `ye` 지움 → `"e"`
- 남음 → 불가능

<br>

**3. 배열은 for문으로 하나씩 본다**

```java
for (String word : babbling) {
    // 각 word 검사
}
```

`babbling[i]`처럼 인덱스로 써도 된다

---

## 오답노트

**1. 처음에 문법부터 막혔다**

```java
for (i = 0; i < ?; i++)
babbling equals[answer] = ;
```

- `for`에 타입이 없음 → `for (int i = 0; i < babbling.length; i++)`
- `equals`는 비교 메서드 이름이지 대입이 아님
- `babbling[i]`로 단어 하나를 꺼내야 함

<br>

**2. 단어 전체가 발음 조합인지 어떻게 보는지 몰랐다**

`aya`, `ye` 같은 발음이 **포함**돼 있으면 되는 게 아니다

단어 전체가 발음들로 **딱 나뉘어야** 한다

그래서 발음을 지워 보고, 남는 글자가 없는지 확인한다

---

## 정답

```java
class Solution {
    public int solution(String[] babbling) {
        int answer = 0;
        String[] words = {"aya", "ye", "woo", "ma"};

        for (String word : babbling) {
            String temp = word;

            for (String w : words) {
                temp = temp.replace(w, " ");
            }

            temp = temp.replace(" ", "");

            if (temp.length() == 0) {
                answer++;
            }
        }

        return answer;
    }
}
```
