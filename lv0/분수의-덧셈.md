# 코딩테스트 연습 - 분수의 덧셈 (LV.0)



## 풀이

**1. 분모를 곱해서 통분한 뒤 분자를 더한다.**

`denom = denom1 * denom2` → 공통 분모를 만든다.

`numer = numer1 * denom2 + numer2 * denom1` → 통분한 분자끼리 더한다.

이렇게 하면 두 분수의 합이 `numer / denom` 형태가 된다고 생각하였다.

<span style="color:red">ex)</span> `1/2 + 3/4`

- 분모: `2 * 4 = 8`
- 분자: `1*4 + 3*2 = 10`
- 합: `10/8` (아직 약분 전)

<br>

**2. GCD(최대공약수)로 약분한다.**

더한 직후에는 기약분수가 아닐 수 있다.

그래서 `g = gcd(numer, denom)`을 구한 뒤, 분자·분모를 각각 `g`로 나눈다.

`int[] answer = {numer / g, denom / g}`로 담아서 `return answer` 하면 된다고 생각하였다.

<span style="color:red">ex)</span> `10/8`

- `gcd(10, 8) = 2`
- `10/2 = 5`, `8/2 = 4`
- 결과: `[5, 4]`

<br>

**3. gcd는 유클리드 호제법으로 구한다.**

`a % b`로 나머지를 구한 뒤, `a = b`, `b = 나머지`로 바꿔가며 반복한다.

`b == 0`이 되면 그때의 `a`가 최대공약수다.

<span style="color:red">ex)</span> `gcd(12, 8)`

- `12 % 8 = 4` → a=8, b=4
- `8 % 4 = 0` → a=4, b=0
- 결과: `4`

---

## 오답노트

**1. LCM으로 통분하면 알아서 기약분수가 된다고 생각했다.**

분모 둘의 최소공배수(LCM)로 통분하면 약분이 필요 없을 줄 알았다.

하지만 LCM을 써도 약분이 필요할 수 있다.

<span style="color:red">ex)</span> `1/6 + 1/3`

- LCM으로 통분해도 `3/6`이 될 수 있음
- 기약분수로 만들려면 한 번 더 약분해서 `1/2`

결국 **통분해서 더한 뒤 GCD로 약분**하는 게 정석이다.

<br>

**2. 소수(2, 3, 5, 7…)로 직접 나눠서 약분하려고 했다.**

되는 경우도 있지만, 빠뜨리거나 비효율적이다.

최대공약수 한 번으로 나누는 편이 깔끔하다.

<br>

**3. `gcd(numer, denom)`만 호출하고 메서드 정의를 안 했다.**

`cannot find symbol` 에러가 났다.

`solution` 안에서 부르려면, 같은 클래스 안에 `gcd` 메서드를 **따로 정의**해야 한다.

`solution` 메서드 **밖**, `Solution` 클래스 **안**에 두면 된다.

<br>

**4. `return`을 두 번 썼다.**

`return new int[]{...}`와 `return answer`를 같이 쓰면 안 된다.

`return`은 함수를 끝내면서 값을 한 번만 넘긴다.

`int[] answer = {...}`에 담아 두고 `return answer` 한 번만 쓰는 형태로 정리했다.

---

## 정답

```java
class Solution {
    public int[] solution(int numer1, int denom1, int numer2, int denom2) {
        int numer = numer1 * denom2 + numer2 * denom1;
        int denom = denom1 * denom2;

        int g = gcd(numer, denom);

        int[] answer = {numer / g, denom / g};
        return answer;
    }

    int gcd(int a, int b) {
        while (b != 0) {
            int r = a % b;
            a = b;
            b = r;
        }
        return a;
    }
}
```
