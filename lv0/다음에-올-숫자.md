# 코딩테스트 연습 - 다음에 올 숫자 (LV.0)

## 풀이

**1. 등차면 뺀 값이 같고, 등비면 나눈 값이 같다**

`common`은 등차수열 아니면 등비수열이다

앞에서 세 칸만 비교하면 된다

```python
common[1] - common[0] == common[2] - common[1]
```

같으면 등차, 아니면 등비라고 생각하였다

<span style="color:red">ex)</span> `[1, 2, 3, 4]`

- `2-1 = 1`, `3-2 = 1` → 같음 → 등차
- 마지막 `4`에 공차 `1`을 더함 → `5`

<span style="color:red">ex)</span> `[2, 4, 8]`

- `4-2 = 2`, `8-4 = 4` → 다름 → 등비
- 마지막 `8`에 공비 `2`를 곱함 → `16`

<br>

**2. 다음에 올 숫자는 마지막 값에 공차/공비를 적용한다**

리스트 길이가 제각각이라서, 마지막이 몇 번 칸인지 매번 다르다

- 길이 4면 마지막은 `[3]`
- 길이 3면 마지막은 `[2]`

파이썬은 맨 뒤를 `common[-1]`로 꺼낸다

자바의 `common[common.length - 1]`이랑 같다

등차면

```python
d = common[1] - common[0]
return common[-1] + d
```

등비면

```python
r = common[1] // common[0]
return common[-1] * r
```

---

## 오답노트

**1. `common`이 배열 종류인 줄 알았다**

파이썬이라서 `array` 대신 `common`을 쓰는 줄 알았다

그냥 문제에서 지어 준 **변수 이름**이다

파이썬에서 여러 칸을 담는 건 리스트고, 그걸 `common`이라고 받은 것이다

<br>

**2. 마지막 칸을 어떻게 꺼내는지 몰랐다**

처음은 `common[0]`이라서 괜찮은데, 마지막 번호는 길이가 제각각이라 모른다

그래서 `common[-1]`을 쓴다

`common([-1])`처럼 괄호를 치면 안 되고, `common[-1]`이다

`len(common) - 1`로 마지막 인덱스를 구해도 같다

---

## 정답

```python
def solution(common):
    if common[1] - common[0] == common[2] - common[1]:
        d = common[1] - common[0]
        return common[-1] + d
    else:
        r = common[1] // common[0]
        return common[-1] * r
```
