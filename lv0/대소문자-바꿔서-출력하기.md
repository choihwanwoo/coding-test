# 코딩테스트 연습 - 대소문자 바꿔서 출력하기 (LV.0)

## 풀이

**1. 대문자는 소문자로, 소문자는 대문자로 바꿔서 출력한다.**

영문만 들어 있는 문자열 `str`을 받아서, 대소문자를 뒤집은 뒤 출력하면 된다.

<span style="color:red">ex)</span> `aBcDeFg` → `AbCdEfG`

<br>

**2. Python에는 대소문자 바꾸는 메서드가 있다.**

인터넷에서 찾아보니 이런 것들이 있었다.

| 메서드 | 역할 |
|--------|------|
| `swapcase()` | 대↔소 뒤집기 |
| `upper()` | 전부 대문자 |
| `lower()` | 전부 소문자 |

이 문제는 한 글자씩 뒤집으면 되니까 `swapcase()`가 맞다.

```python
str = input()
str = str.swapcase()
print(str)
```

`input()`으로 문자열을 받고, 바꾼 뒤 `print`로 출력한다.

---

## 오답노트

**1. 처음에는 메서드를 몰라서 인터넷에서 찾았다.**

자바만 하다가 파이썬으로 풀려니, 대소문자 바꾸는 함수 이름부터 몰랐다.

`swapcase`, `upper`, `lower`를 보고 바로 적용하니까 됐다.

<br>

**2. 파이썬 문법이 익숙하지 않아서 자꾸 자바처럼 쓰게 된다.**

자바만 해봐서 파이썬이 헷갈렸다.

예를 들면:

| 자바 | 파이썬 |
|------|--------|
| `Scanner`로 입력 | `input()` |
| `System.out.println` | `print()` |
| `str.toUpperCase()` | `str.upper()` |
| 세미콜론 `;` 필수 | 세미콜론 보통 안 씀 |
| 중괄호 `{}`로 블록 | 들여쓰기로 블록 |

문법은 다른데, “문자열에 메서드를 붙여서 변환한다”는 느낌은 비슷하다.

파이썬은 더 짧게 쓰는 편이라, 자바식 긴 코드를 그대로 옮기려 하면 더 헷갈린다.

---

## 정답

```python
str = input()
str = str.swapcase()
print(str)
```
