# 코딩테스트 연습 - 최빈값 구하기 (LV.0)

## 풀이

**1. 최빈값은 “가장 많이 나온 값”이다.**

배열에서 각 숫자가 몇 번 나왔는지 센 뒤, 횟수가 가장 큰 숫자를 고른다.

단, 횟수가 같은 최댓값이 **둘 이상**이면 `-1`을 반환해야 한다.

<span style="color:red">ex)</span>

- `[1, 2, 3, 3, 3, 4]` → `3`이 3번 → `3`
- `[1, 1, 2, 2]` → `1`과 `2`가 둘 다 2번 → `-1`
- `[1]` → `1`

<br>

**2. 제한사항을 이용해서 카운트 배열을 만든다.**

원소 범위가 `0 ≤ 원소 < 1000`이라서 `int[] count = new int[1000]`을 쓸 수 있다.

`count[숫자] = 그 숫자가 나온 횟수`로 둔다.

```java
int[] count = new int[1000];

for (int i = 0; i < array.length; i++) {
    count[array[i]]++;
}
```

<span style="color:red">ex)</span> `array = [1, 2, 3, 3, 3, 4]`

- `count[1] = 1`
- `count[2] = 1`
- `count[3] = 3`
- `count[4] = 1`

<br>

**3. 최대 횟수를 찾으면서 최빈값을 정한다.**

`count`를 0부터 999까지 돌면서:

- 횟수가 `max`보다 크면 → `max`와 `answer`를 갱신
- 횟수가 `max`와 같으면 → 동점이므로 `answer = -1`

```java
int max = 0;
int answer = 0;

for (int i = 0; i < 1000; i++) {
    if (count[i] > max) {
        max = count[i];
        answer = i;
    } else if (count[i] == max) {
        answer = -1;
    }
}
```

처음엔 `max = 0`이라서, 한 번도 안 나온 숫자(`count[i] == 0`)가 `== max`로 걸려도  
아직 진짜 값이 나오기 전이고, 값이 나온 뒤에는 `max`가 1 이상이 되어 동점만 `-1`로 바뀐다.

---

## 오답노트

**1. 중앙값이랑 헷갈렸다.**

중앙값 → 정렬 후 **가운데**  
최빈값 → **가장 많이 나온** 값

정렬만 해서 가운데를 고르면 이 문제는 틀리다.

<br>

**2. 동점일 때 `-1`을 빼먹었다.**

가장 많이 나온 값만 찾고 끝내면, `[1, 1, 2, 2]`에서 `1` 또는 `2`를 반환해서 틀린다.

최대 횟수와 **같은 횟수가 또 있는지** 반드시 확인해야 한다.

<br>

**3. `count[array[i]]++`가 처음에 낯설었다.**

`array[i]`가 `3`이면 `count[3]`을 1 늘린다는 뜻이다.

숫자를 **배열 인덱스**로 써서 횟수를 세는 방식이다.  
범위가 0~999로 작을 때 쓰기 좋다.

이중 for로 직접 세도 되지만, 이 방식이면 for 한 번 + 확인 for 한 번으로 끝난다.

---

## 정답

```java
class Solution {
    public int solution(int[] array) {
        int[] count = new int[1000];

        for (int i = 0; i < array.length; i++) {
            count[array[i]]++;
        }

        int max = 0;
        int answer = 0;

        for (int i = 0; i < 1000; i++) {
            if (count[i] > max) {
                max = count[i];
                answer = i;
            } else if (count[i] == max) {
                answer = -1;
            }
        }

        return answer;
    }
}
```
