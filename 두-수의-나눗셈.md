# [프로그래머스] 두 수의 나눗셈 — float/double과 (int) 캐스팅

## 문제 요약

정수 `num1`, `num2`가 주어지면  
`(num1 / num2) × 1000`을 계산한 뒤 **정수 부분만** 반환한다.

| num1 | num2 | result |
|------|------|--------|
| 3 | 2 | 1500 |
| 7 | 3 | 2333 |
| 1 | 16 | 62 |

예: `7 / 3 = 2.333...` → `× 1000 = 2333.333...` → 정수 부분 `2333`

---

## 정답 코드

```java
class Solution {
    public int solution(int num1, int num2) {
        int answer = (int) ((double) num1 / num2 * 1000);
        return answer;
    }
}
```

`float`을 써도 된다.

```java
int answer = (int) ((float) num1 / num2 * 1000);
```

---

## 왜 이렇게 해야 하나?

### 1. `int / int`는 소수점이 안 나온다

자바에서 정수끼리 나누면 **몫만** 남긴다.

```java
7 / 3  // → 2  (나머지 버림)
```

그래서 그냥 `num1 / num2 * 1000` 하면 `2 * 1000 = 2000`이 되어 틀리다.

### 2. `float` / `double`로 실수 나눗셈을 시킨다

`float`, `double`은 **소수까지 담는 실수형**이다.

```java
(double) num1 / num2  // → 2.333...
```

한쪽만 실수로 바꿔도 나눗셈이 실수 연산이 된다.

### 3. `(int)`로 정수 부분만 남긴다

`float`/`double` 자체가 정수만 뽑는 게 아니다.  
**정수 부분만 필요한 순간 `(int)`로 캐스팅**한다.

```java
(int) 2333.333...  // → 2333  (소수점 버림, 반올림 아님)
```

흐름 정리:

1. `(double)` → 소수까지 나누기  
2. `* 1000` → 문제 요구대로 스케일업  
3. `(int)` → 정수 부분만 반환

---

## 오답 노트

### 내가 틀렸던 코드

```java
class Solution {
    public int solution(int num1, int num2) {
        int answer = 0;
        answer = (float) num1 / (float) num2 * 1000;  // ❌ 실수 → 정수 대입
        return answer;
    }
}
```

### 실수 1. float/double을 몰랐다

실수 나눗셈이 필요하다는 건 알았는데,  
`float`/`double`이 **"소수 버리는 함수"가 아니라 "소수 포함한 실수 타입"**인 줄 몰랐다.

| 타입 | 의미 |
|------|------|
| `int` | 정수 |
| `float` | 실수 (대략 소수점 7자리) |
| `double` | 실수 (더 정밀, 보통 이걸 더 많이 씀) |

### 실수 2. `int`를 잘못 이해했다

처음엔 `int`를 **함수 선언/호출할 때만 쓰는 것**으로 알았다.

실제로는 `int`는 **타입**이다.

```java
public int solution(...) {   // 반환 타입이 정수
    int answer = 0;          // 정수 변수 선언
    return (int) 62.5;       // 실수 → 정수로 변환 (버림)
}
```

- 함수 **이름**이 아님  
- 함수를 **호출**하는 키워드도 아님  
- **"이 값은 정수다"**라고 알려주는 자료형

그래서 마지막에 `(int)`를 붙이는 것도  
"정수 부분만 남겨서 `int` 타입으로 만들자"는 뜻이다.

### 실수 3. 형 변환을 안 맞췄다

`answer`는 `int`(정수 상자)인데, 오른쪽 계산 결과는 `float`(실수)다.  
자바는 **타입이 다른 값을 그냥 넣지 못하게** 막는다.

#### 예시로 보기 (`num1 = 7`, `num2 = 3`)

```java
(float) num1 / (float) num2 * 1000
// 7.0 / 3.0 * 1000
// → 2333.3333...   ← 아직 float(실수)
```

이 값을 `int answer`에 그대로 넣으면?

```java
int answer = 0;
answer = (float) num1 / num2 * 1000;  // ❌ 컴파일 에러
// incompatible types: possible lossy conversion from float to int
```

에러 뜻: `float` → `int`로 넣으면 **소수점이 잘려서 정보가 손실**될 수 있으니,  
자바가 "진짜 그렇게 할 거면 네가 직접 `(int)`로 말해"라고 요구하는 것이다.

#### 올바른 방법

```java
// ✅ (int)로 "소수 버리고 정수로 바꾸겠다"고 명시
answer = (int) ((float) num1 / num2 * 1000);
// 2333.3333... → 2333
```

다른 예시도 같다.

```java
int a = 62.5;        // ❌ 안 됨 (double → int)
int b = (int) 62.5;  // ✅ 62 (소수점 버림)

float f = 1500.0f;
int c = f;           // ❌ 안 됨
int d = (int) f;     // ✅ 1500
```

정리:

1. `(float)` / `(double)` → 나눗셈을 **실수로** 계산하게 함  
2. `(int)` → 그 결과를 **정수 상자에 넣을 수 있게** 바꿈 (소수 버림)  
3. 둘 다 필요한 이유: 하나는 **계산용**, 하나는 **담기용**

---

## 한 줄 요약

> `int/int`는 몫만 나오니까 → `float`/`double`로 소수 계산 → `×1000` → `(int)`로 소수 버림
