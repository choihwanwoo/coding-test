# 코딩테스트 연습 - 정수를 나선형으로 배치하기 (LV.0)

이번 문제는 감이 잘 안 와서 거의 손도 못 댔다  
`n×n` 판에 `1`부터 `n²`까지 달팽이처럼 채우는 문제였다

## 풀이

**1. 오른쪽 → 아래 → 왼쪽 → 위 순서로 채운다**

`(0, 0)`부터 시계 방향으로 숫자를 넣는다

벽 밖이거나 이미 채워진 칸을 만나면 방향을 바꾼다

<span style="color:red">ex)</span> `n = 4`

```
1  → 2  → 3  → 4
               ↓
12 → 13 → 14   5
↑         ↓    ↓
11   16 ← 15   6
↑              ↓
10 ← 9  ← 8  ← 7
```

<br>

**2. 먼저 `n×n` 빈 판을 만든다**

초기 코드 `answer = [[]]`는 빈 배열이라 숫자를 넣을 칸이 없다

```python
answer = [[0] * n for _ in range(n)]
```

전부 `0`으로 채워 두고, 숫자를 넣으면서 `0`이 아닌 칸은 이미 쓴 칸으로 본다

<br>

**3. 네 방향 이동량을 배열로 둔다**

| 순서 | 방향 | dx | dy |
|------|------|----|----|
| 0 | 오른쪽 | 0 | 1 |
| 1 | 아래 | 1 | 0 |
| 2 | 왼쪽 | 0 | -1 |
| 3 | 위 | -1 | 0 |

```python
dx = [0, 1, 0, -1]
dy = [1, 0, -1, 0]
```

지금 방향이 `direction`이면 다음 칸은

```python
nx = x + dx[direction]
ny = y + dy[direction]
```

<br>

**4. 다음 칸이 못 가면 방향을 바꾼다**

못 가는 경우

- `nx`, `ny`가 0보다 작거나 `n` 이상
- `answer[nx][ny]`가 이미 0이 아님

그때

```python
direction = (direction + 1) % 4
```

`% 4`를 쓰면 `0 → 1 → 2 → 3 → 0`으로 다시 오른쪽이 된다

`1`부터 `n*n`까지 현재 칸에 넣고, 다음 칸으로 이동하면 된다

---

## 오답노트

**1. 감이 안 와서 손도 못 댔다**

2차원 배열에 나선형으로 채운다는 말만 보고는 뭘 해야 할지 몰랐다

핵심은 한 줄로 이랬다

> 한 방향으로 가다가, 막히면 시계 방향으로 튼다

<br>

**2. `answer = [[]]`를 그대로 두면 안 된다**

칸이 없는 빈 배열이다

`n×n`을 먼저 만들어야 숫자를 넣을 수 있다

<br>

**3. `dx`, `dy`가 처음엔 낯설었다**

`x`, `y`를 직접 if로 네 번 나누는 대신  
방향 번호에 맞는 이동량을 배열에서 꺼내는 방식이다

자바였어도 같은 생각을 쓴다

---

## 정답

```python
def solution(n):
    answer = [[0] * n for _ in range(n)]
    dx = [0, 1, 0, -1]
    dy = [1, 0, -1, 0]

    x, y = 0, 0
    direction = 0

    for num in range(1, n * n + 1):
        answer[x][y] = num

        nx = x + dx[direction]
        ny = y + dy[direction]

        if nx < 0 or nx >= n or ny < 0 or ny >= n or answer[nx][ny] != 0:
            direction = (direction + 1) % 4
            nx = x + dx[direction]
            ny = y + dy[direction]

        x, y = nx, ny

    return answer
```
