# 코딩테스트 연습 - [PCCE 기출문제] 5번. 심폐소생술 (LV.0)

## 풀이

**1. 올바른 순서를 기준으로 번호를 매긴다.**

심폐소생술 순서는 아래와 같다.

1. `check`
2. `call`
3. `pressure`
4. `respiration`
5. `repeat`

입력 `cpr`은 이 다섯 단어가 섞여 들어온다.  
각 단어가 몇 번째 순서인지 배열로 return 하면 된다고 생각하였다.

<span style="color:red">ex)</span> `["call", "respiration", "repeat", "check", "pressure"]`  
→ `[2, 4, 5, 1, 3]`

<br>

**2. 빈칸 채우기 코드를 이해한다.**

이미 `basic_order`에 정답 순서가 들어 있다.

`String[] basic_order = {"check", "call", "pressure", "respiration", "repeat"};`

- 바깥 `for(int i=0; i <` <code><span style="background:#fff;color:#fff;border:1px solid #ddd;border-radius:4px;padding:2px 24px;">&nbsp;&nbsp;&nbsp;</span></code> `; i++)` → `cpr`의 각 단어를 본다.
- 안쪽 `for(int j=0; j <` <code><span style="background:#fff;color:#fff;border:1px solid #ddd;border-radius:4px;padding:2px 24px;">&nbsp;&nbsp;&nbsp;</span></code> `; j++)` → `basic_order`에서 같은 단어를 찾는다.
- `answer[i] =` <code><span style="background:#fff;color:#fff;border:1px solid #ddd;border-radius:4px;padding:2px 24px;">&nbsp;&nbsp;&nbsp;</span></code> → 찾은 순서 번호를 넣는다.

<br>

**3. 빈칸에 들어갈 값**

- 첫 번째 빈칸: `cpr` 길이만큼 반복 → `cpr.length`
- 두 번째 빈칸: `basic_order` 길이만큼 반복 → `basic_order.length`
- 세 번째 빈칸: 순서는 1부터이므로 인덱스 `j`에 1을 더함 → `j + 1`

`j`는 0부터 시작하니까 `check`일 때 `j = 0` → `1`이 나와야 한다.

---

## 오답노트

**1. `i`랑 `j`를 헷갈림**

처음에는 `i`가 정답 순서(`check`부터)를 도는 줄 알았다.

실제로는:

- `i` → 섞인 입력 `cpr`에서 지금 볼 단어
- `j` → 그 단어가 `basic_order`에서 몇 번째인지 찾기

<span style="color:red">ex)</span> `["call", "respiration", "repeat", "check", "pressure"]`

- `i = 0`일 때 `cpr[0] = "call"`
- `j` 루프로 `basic_order` 5칸을 뒤져서 `"call"` 위치를 찾음
- 한 단어 찾았으면, 다음 단어도 똑같이 찾아야 하니까 `i` 루프를 5번 반복

정리하면:

- 안쪽 `for(j)` = 한 단어를 정답표에서 검색
- 바깥 `for(i)` = 그 검색을 단어 5개에 대해 반복

<br>

**2. 세 번째 빈칸에 `j`만 넣음**

기댓값: `[2, 4, 5, 1, 3]`  
실제 출력: `[1, 3, 4, 0, 2]`

배열 인덱스는 0부터라서 `j`만 넣으면 번호가 전부 1씩 작게 나온다.

- `"call"` → `j = 1`인데 정답 순서는 **2**
- `"check"` → `j = 0`인데 정답 순서는 **1**

그래서 `j + 1`을 써야 한다.

---

## 정답

```java
class Solution {
    public int[] solution(String[] cpr) {
        int[] answer = {0, 0, 0, 0, 0};
        String[] basic_order = {"check", "call", "pressure", "respiration", "repeat"};

        for(int i=0; i < cpr.length; i++){
            for(int j=0; j < basic_order.length; j++){
                if(cpr[i].equals(basic_order[j])){
                    answer[i] = j + 1;
                    break;
                }
            }
        }
        return answer;
    }
}
```
