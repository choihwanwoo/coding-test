# 코딩테스트 연습 - [PCCE 기출문제] 4번. 병과분류 (LV.0)

## 풀이

**1. 마지막 네 글자를 잘라서 비교한다.**

`code.substring(code.length()-4, code.length())`로 끝 4글자를 `lastFourWords`에 담는다.

그다음 `lastFourWords`가 표의 단어와 같은지 확인해서 병과를 출력하면 된다고 생각하였다.

| 마지막 글자 | 병과 |
| --- | --- |
| `"_eye"` | `"Ophthalmologyc"` |
| `"head"` | `"Neurosurgery"` |
| `"infl"` | `"Orthopedics"` |
| `"skin"` | `"Dermatology"` |
| 그 외 | `"direct recommendation"` |

<br>

**2. 빈칸에는 비교식을 채운다.**

첫 빈칸은 `equals` 안에 넣을 문자열 → `"_eye"`

나머지 `else if` 빈칸은 조건 전체 → `lastFourWords.equals("head")` 형태

마지막은 위에 해당 안 될 때 → `else`

<span style="color:red">ex)</span> `dry_eye` → 끝 4글자 `"_eye"` → `Ophthalmologyc`

---

## 오답노트

**1. `_eye` (따옴표 없음)**

문자열인데 변수처럼 써서 안 됐다.

`"_eye"`처럼 큰따옴표가 필요하다.

<br>

**2. `else if("head")`**

에러: `incompatible types: String cannot be converted to boolean`

`if` 안에는 참/거짓만 올 수 있다. `"head"`는 문자열이라 조건이 될 수 없다.

`lastFourWords.equals("head")`처럼 비교해야 한다.

---

## 정답

```java
import java.util.Scanner;

public class Solution {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String code = sc.next();
        String lastFourWords = code.substring(code.length()-4, code.length());

        if(lastFourWords.equals("_eye")){
            System.out.println("Ophthalmologyc");
        }
        else if(lastFourWords.equals("head")){
            System.out.println("Neurosurgery");
        }
        else if(lastFourWords.equals("infl")){
            System.out.println("Orthopedics");
        }
        else if(lastFourWords.equals("skin")){
            System.out.println("Dermatology");
        }
        else {
            System.out.println("direct recommendation");
        }
    }
}
```
