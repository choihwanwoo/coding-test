# 코딩테스트 연습 - 점 찍기 (LV.2)

## 풀이

**1. `k` 간격으로 점을 찍고, 원점 거리가 `d` 이하인 것만 센다.**

점 좌표는 `(a × k, b × k)` 이고, `a`, `b`는 0 이상 정수다.

원점 `(0, 0)` 과의 거리가 `d` 이하인 점의 개수를 구하면 된다.

<span style="color:red">ex)</span>

- `k = 2`, `d = 4` → `6`
- `k = 1`, `d = 5` → `26`

<br>

**2. 제곱은 격자 칸 수가 아니라, 원 안인지 보는 데 쓴다.**

원점에서의 거리 제곱은 `x² + y²` 이다.

이게 `d` 이하여야 하니까:

```
x² + y² ≤ d²
```

`d`는 **원의 반지름**, `x² + y² = d²` 은 **원의 경계**다.

그래서 `(d / k)` 를 제곱해서 점 개수를 구하는 게 아니라,  
각 `x`마다 원 안에 들어가는 `y`가 몇 개인지 세야 한다.

`x`를 `0, k, 2k, ...` 로 두고,

```
y 최댓값 = √(d² - x²)
개수 = (y 최댓값 / k) + 1
```

`+ 1` 은 `y = 0` 도 포함하기 때문이다.

<span style="color:red">ex)</span> `k = 1`, `d = 5`

- x=0 → y 최대 5 → 6개
- x=1 → y 최대 4 → 5개
- x=2 → y 최대 4 → 5개
- x=3 → y 최대 4 → 5개
- x=4 → y 최대 3 → 4개
- x=5 → y 최대 0 → 1개
- 합: `26`

<br>

**3. `d * d` 는 `long`으로 계산한다.**

`d` 최댓값이 `1,000,000` 이라 `d * d` 를 `int`로 하면 오버플로가 난다.

`Math.sqrt` 결과는 `double` 이라서, 개수에 더할 때는 `long`으로 바꾼다.

---

## 오답노트

**1. 처음에 `d / k` 를 제곱하고 `k` 를 더하면 되는 줄 알았다.**

```java
long c = d / k;
double e = Math.pow(c, 2);
answer = (long) (e + k);
```

제곱하는 발상은 좋았다.

그런데 그 제곱을 **한 변 × 한 변** 같은 격자 개수로 생각했다.

실제로는 원의 반지름으로 **원의 경계**를 구하는 제곱이었다.

<span style="color:red">ex)</span>

- `k=2, d=4` → `2² + 2 = 6` → 우연히 맞음
- `k=1, d=5` → `5² + 1 = 26` → 이것도 우연히 맞음

예제는 통과해 보이지만, 원 밖에 잘리는 점이 있는 케이스는 전부 틀린다.

<br>

**2. `double` 을 `long` 에 그대로 넣어서 컴파일 에러가 났다.**

```java
answer = e + k;  // ❌ possible lossy conversion from double to long
```

`Math.pow` 결과는 `double` 이다.

`long` 에 넣으려면 `(long)` 으로 직접 변환해야 한다.

이건 형 변환 문제고, 그걸 고쳐도 공식 자체가 틀려서 테스트는 실패한다.

---

## 정답

```java
class Solution {
    public long solution(int k, int d) {
        long answer = 0;
        long dd = (long) d * d;

        for (long x = 0; x <= d; x += k) {
            long maxY = (long) Math.sqrt(dd - x * x);
            answer += (maxY / k) + 1;
        }

        return answer;
    }
}
```

### 한 줄 요약

> 제곱은 격자 개수가 아니라 피타고라스다. `x`마다 원 안의 `y` 개수를 더한다.
