![](https://velog.velcdn.com/images/choihwanwoo/post/6b844451-63c3-4d0b-80da-b0ee5cb62b3b/image.png)

![](https://velog.velcdn.com/images/choihwanwoo/post/e68a60b8-3f85-4da8-9aaa-32bba8e4d1b4/image.png)

---

## ✍️ 풀이

**1. 짝수면 `n is even`, 홀수면 `n is odd`를 출력한다**

자연수 `n`을 입력받아서, 2로 나눈 나머지로 홀짝을 구분하면 된다.

<span style="color:red">ex)</span>

- `100` → `100 is even`
- `1` → `1 is odd`

<br>

**2. Python은 `input()`으로 받고, `if`는 `:`와 들여쓰기를 쓴다**

```python
a = int(input())

if a % 2 == 0:
    print(f"{a} is even")
else:
    print(f"{a} is odd")
```

- `int(input())` → 문자열로 들어온 입력을 정수로 바꿈
- `a % 2 == 0` → 나머지가 0이면 짝수
- `print(f"{a} is even")` → 숫자와 글을 같이 출력

`print(a, "is even")`도 된다. 쉼표로 넣으면 공백이 자동으로 들어간다.

---

## 🗒️ 오답노트

**1. 자바 문법을 파이썬에 그대로 썼다**

처음엔 이렇게 짰다.

```python
if (a % 2 = 0){
    printf(a + "is even")
}
else{
    printf(a + "is odd")
}
```

파이썬이랑 안 맞는 부분

| 자바 버릇 | 파이썬 |
|-----------|--------|
| `{ }` | `:` + 들여쓰기 |
| `=` (비교하려고 씀) | 비교는 `==` |
| `printf` | `print` |

에러 메시지도 `Maybe you meant '==' instead of '='?` 라고 나왔다.

<br>

**2. `==`랑 `:`는 고쳤는데, `a + " is even"`에서 또 막혔다**

```python
print(a + " is even")
```

자바에서는 숫자 + 문자열이 자동으로 이어진다.

파이썬에서는 `int`랑 `str`을 `+`로 못 붙인다. `TypeError`가 난다.

숫자를 글자로 바꾸거나, `print`에 따로 넘겨야 한다.

```python
print(str(a) + " is even")
print(a, "is even")
print(f"{a} is even")
```

자바만 하다가 파이썬 하니까, 문법 차이가 계속 헷갈렸다.

---

## ✅ 정답

```python
a = int(input())

if a % 2 == 0:
    print(f"{a} is even")
else:
    print(f"{a} is odd")
```
