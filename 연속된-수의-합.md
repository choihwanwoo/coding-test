# 코딩테스트 연습 - 연속된 수의 합 (LV.0)

## 풀이

**1. 연속된 `num`개의 합이 `total`이 되는 수열을 구한다**

`n`, `n+1`, `n+2` … 이런 식으로 `num`개를 더해서 `total`이 되게 만들면 된다

오름차순 배열로 반환하면 된다

<span style="color:red">ex)</span>

- `num=3`, `total=12` → `[3, 4, 5]`
- `num=5`, `total=15` → `[1, 2, 3, 4, 5]`
- `num=5`, `total=5` → `[-1, 0, 1, 2, 3]`

<br>

**2. 시작값 `n`만 구하면 된다**

```
n + (n+1) + (n+2) + ... + (n+num-1) = total
= num*n + (0+1+...+num-1)
= num*n + num*(num-1)/2
```

양쪽을 `num`으로 나누고, `(num-1)/2`를 오른쪽으로 넘기면

```
n = total / num - (num-1)/2
```

코드에서는 몫만 쓰려고 `//`를 쓴다

```python
n = total // num - (num - 1) // 2
```

<span style="color:red">ex)</span> `num=3`, `total=12`

- `12/3 - (3-1)/2 = 4 - 1 = 3`
- `n=3` → `3, 4, 5`

<br>

**3. `n`, `n+1`, `n+2` … 를 `num`번 배열에 넣는다**

합이 `total`이 될 때까지 더하는 게 아니다

이미 `n`을 공식으로 맞춰 놨으니, `n+i`를 `num`번 넣으면 합은 자동으로 `total`이 된다

```python
for i in range(num):
    answer.append(n + i)
```

- `i=0` → `n`
- `i=1` → `n+1`
- `i=2` → `n+2`
- …

`num`은 몇 개를 넣을지고, `i`만큼 더한 값을 넣는 것이다

---

## 오답노트

**1. 식을 `n = total/num - (num-1)/2`로 바꾸는 게 안 보였다**

`num*n + num*(num-1)/2 = total` 까지는 이해가 됐다

그다음이 갑자기 나와서 헷갈렸다

그냥 이항이다

- 양쪽을 `num`으로 나눈다
- 남은 `(num-1)/2`를 오른쪽으로 넘기면 빼기

<br>

**2. for문을 합이 `total`될 때까지 더하는 줄 알았다**

아니다

`n+i`를 `num`번 배열에 넣는 것이다

`total`은 루프 조건이 아니라, `n` 구할 때 이미 쓴 값이다

<br>

**3. 파이썬은 `//`가 몫이다**

자바의 `int / int`처럼 쓰려면 `/`가 아니라 `//`를 써야 한다

`/`는 실수 나눗셈이 된다

---

## 정답

```python
def solution(num, total):
    n = total // num - (num - 1) // 2
    answer = []

    for i in range(num):
        answer.append(n + i)

    return answer
```
