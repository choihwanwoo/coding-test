# 코딩테스트 연습 - 문자 리스트를 문자열로 변환하기 (LV.0)

## 풀이

**1. 문자 배열을 이어 붙여서 하나의 문자열로 만들면 된다.**

`arr`에 문자가 순서대로 들어 있고, 그걸 합쳐서 문자열로 반환한다.

<span style="color:red">ex)</span> `["a", "b", "c"]` → `"abc"`

<br>

**2. Python에서는 `''.join(arr)` 로 붙인다.**

```python
answer = ''.join(arr)
```

`''` 는 사이사이에 넣을 문자다.  
아무것도 없는 빈 문자열이라서, 글자만 쭉 이어진다.

`"-"`.join` 이면 `"a-b-c"` 가 된다.

---

## 오답노트

처음에는 어떻게 이어 붙여야 하는지 몰랐다.

인터넷에서 `.join` 을 보고 바로 썼더니 통과했다.

배열/리스트 원소를 문자열로 합칠 때 Python에서는 `join` 을 쓰면 된다.

---

## 정답

```python
def solution(arr):
    answer = ''.join(arr)
    return answer
```
